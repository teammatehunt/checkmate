{
  "name": "checkmate-extension",
  "version": "0.1.0",
  "description": "Helper for iframe management for puzzle solving.",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "web-ext": "^5.4.0",
    "webextension-polyfill": "^0.7.0"
  },
  "scripts": {
    "start:firefox": "web-ext run --source-dir ./src/ -t firefox-desktop",
    "start:chrome": "web-ext run --source-dir ./src/ -t chromium",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brian Shimanuki",
  "license": "MIT"
}
